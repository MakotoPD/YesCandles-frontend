// This plugin ensures that all Nuxt UI components are properly registered
import { defineNuxtPlugin } from '#app'

export default defineNuxtPlugin((nuxtApp) => {
  // No need to manually register UI components as they should be auto-imported by Nuxt UI
  // This plugin is just a placeholder to ensure the module is loaded correctly
})
